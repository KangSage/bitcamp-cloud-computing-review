// 017. 배열(array)에 값 넣기
"use strict";

// 미리 선언해놓고 차례대로 넣는 방법.
const arNumbers = [];
arNumbers.push(1);
arNumbers.push(2);
arNumbers.push(3);

// 선언해놓고 한번에 밀어 넣는 방법.
const arTexts = [];
arTexts.push('hello', 'welcome', 'bye');

console.log(arNumbers);
console.log(arTexts);

console.log("-------------");

console.log(arNumbers.length);
console.log(arTexts.length);